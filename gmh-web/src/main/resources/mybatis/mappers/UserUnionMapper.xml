<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zes.squad.gmh.mapper.UserUnionMapper">
	<resultMap id="BaseResultMap" type="com.zes.squad.gmh.entity.union.UserUnion">
		<id column="id" jdbcType="BIGINT" property="id" />
		<result column="token" jdbcType="VARCHAR" property="token" />
		<result column="store_name" jdbcType="VARCHAR" property="storeName" />
		<association property="userPo"
			resultMap="com.zes.squad.gmh.mapper.UserMapper.BaseResultMap"
			columnPrefix="_u" />
	</resultMap>
	<select id="selectUserUnionsByIds" parameterType="list" resultMap="BaseResultMap">
		SELECT
		u.`account` AS u_account,
		u.`email` AS u_email,
		u.`mobile` AS u_mobile,
		u.`name` AS u_name,
		u.`gender` AS u_gender,
		u.`store_id` AS u_store_id,
		s.`name` AS store_name
		FROM user u
		LEFT JOIN store s
		ON
		u.`store_id` = s.`id`
		<where>
			u.`id` IN 
			<foreach collection="list" item="id" open="(" close=")" separator=",">
				#{id, jdbcType=BIGINT}
			</foreach>
		</where>
	</select>
</mapper>