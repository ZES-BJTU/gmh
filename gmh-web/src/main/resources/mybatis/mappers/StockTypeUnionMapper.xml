<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zes.squad.gmh.mapper.StockTypeUnionMapper">
	<resultMap id="BaseResultMap" type="com.zes.squad.gmh.entity.union.StockTypeUnion">
		<id column="id" jdbcType="BIGINT" property="id" />
		<result column="store_name" jdbcType="VARCHAR" property="storeName" />
		<association property="stockTypePo"
			resultMap="com.zes.squad.gmh.mapper.StoreMapper.BaseResultMap"
			columnPrefix="stock_type_" />
	</resultMap>
	<sql id="BaseColumnList">
		st.`id` AS stock_type_id,
		st.`name` AS stock_type_name,
		st.`store_id` AS stock_type_store_id,
		st.`created_time` AS stock_type_created_time,
		st.`modified_time` AS stock_type_modified_time,
		s.`name` AS store_name
	</sql>
	<select id="selectById" parameterType="long" resultMap="BaseResultMap">
		SELECT
		<include refid="BaseColumnList" />
		FROM
		store_type st
		LEFT JOIN store s
		ON
		st.`store_id` = s.`id`
		WHERE st.`id` = #{id, jdbcType=BIGINT}
	</select>
	<select id="selectByCondition"
		parameterType="com.zes.squad.gmh.entity.condition.StockTypeQueryCondition"
		resultMap="BaseResultMap">
		SELECT
		<include refid="BaseColumnList" />
		FROM
		store_type st
		LEFT JOIN store s
		ON
		st.`store_id` = s.`id`
		<where>
			<if test="stockTypeKeyWord != null and stockTypeKeyWord != ''">
				<bind name="condition" value="'%' + stockTypeKeyWord + '%'" />
				st.`name` LIKE #{condition, jdbcType=VARCHAR}
			</if>
		</where>
	</select>
</mapper>